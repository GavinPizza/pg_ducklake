# Appended after regression.conf (later --temp-config wins) for the
# WITH_PG_DUCKDB=1 coexistence run: also preload community pg_duckdb so the
# regression suite exercises pg_ducklake with pg_duckdb co-installed.
shared_preload_libraries = 'pg_duckdb,pg_ducklake'
